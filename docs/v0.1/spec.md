# HorizonCode — Spec v0.1

## 背景
从零构建一个终端 AI 编程助手（HorizonCode），第一步先打通"终端交互式对话 + 流式回复"的基础闭环。后续迭代再加 agent 能力（tool use、文件操作、代码编辑等）。

## 目标用户
在终端里工作的开发者，希望通过命令行与 LLM 对话而无需离开终端。

## 能力清单（P0 — 本次交付）

1. **交互式 TUI 对话**：启动后在终端里进入交互界面，支持多行输入（含粘贴），Enter 发送消息，AI 回复流式逐字打印。
2. **多轮对话记忆**：同一会话内 AI 能记住之前的所有对话，每次启动为一个独立会话。
3. **双 Provider 支持**：支持 Anthropic Claude API 和 OpenAI API 两种后端，通过 YAML 配置文件切换。
4. **流式输出 (SSE)**：使用 Server-Sent Events 协议实现流式响应，不等全部生成完再返回。
5. **Extended Thinking 透传**：Claude 的 extended thinking 内容在流式输出中逐字展示，与普通回复做视觉区分。
6. **可中断生成**：在 AI 流式输出期间，用户可通过快捷键中止当前回复。
7. **对话历史持久化**：每次会话的完整对话记录自动保存到磁盘，退出后下次启动可查看历史。
8. **双层配置**：支持用户级配置（`~/.horizoncode/config.yaml`）和项目级配置（`./horizoncode.yaml`），项目级覆盖用户级。

## 非功能要求

- **流式延迟**：首 token 到达后应立即渲染，不做批量缓冲（或缓冲 ≤50ms 以防止闪烁）。
- **终端兼容**：支持主流终端模拟器（iTerm2、Terminal.app、Windows Terminal、WSL、Linux 原生终端），最低 80×24 字符窗口。
- **Python 版本**：≥3.10。
- **配置文件安全**：api_key 支持从环境变量读取（如 `${ENV_VAR}`），不强制明文写在文件中。
- **错误恢复**：API 调用失败时在界面显示错误信息，不崩溃退出；允许用户重试。
- **扩展性**：Provider 层抽象为统一接口，新增第三方 API 后端只需新增一个模块，不改核心逻辑。

## 设计骨架

```
用户输入 (prompt_toolkit)
      │
      ▼
 ┌─────────────┐     ┌──────────────────┐
 │  TUI 层      │────▶│  Provider 路由    │
 │  (渲染/输入)  │     │  (按 config 选择)  │
 └─────────────┘     └──────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼                               ▼
     ┌────────────────┐              ┌────────────────┐
     │ AnthropicProvider│              │  OpenAIProvider │
     │ (SSE stream)    │              │  (SSE stream)   │
     └────────────────┘              └────────────────┘
              │                               │
              └───────────────┬───────────────┘
                              ▼
                     ┌────────────────┐
                     │  流式渲染引擎    │
                     │  (正文 + thinking)│
                     └────────────────┘
                              │
                              ▼
                     ┌────────────────┐
                     │  历史持久化      │
                     │  (JSON 文件)    │
                     └────────────────┘
```

- **TUI 方案**：`prompt_toolkit` 负责输入处理、快捷键绑定、历史回放；`rich` 负责 Markdown 渲染和流式更新。（方案可调，见 tasks.md）
- **Provider 接口**：所有 Provider 实现同一抽象基类，暴露 `stream_chat(messages, model)` 异步生成器方法，yield 的每帧区分 `thinking` 与 `content` 两种类型。
- **配置层**：启动时先读用户级配置，若当前目录存在项目级配置则 merge 覆盖。四个核心字段：`protocol`、`model`、`base_url`、`api_key`，可按多组 profile 配置后用 `default_profile` 指定使用哪组。
- **历史层**：每次会话保存为一个 JSON 文件，格式为消息数组（含角色、内容、时间戳），存储路径可配置。

## Out of Scope（本步不做）

- Tool use / function calling
- 文件读写、代码编辑
- 多会话管理 / 会话切换
- MCP 协议集成
- 对话分支 / 回退到历史某条消息
- 多模态（图片输入）
- 自定义 system prompt（后续加）
- 自动补全 / 代码高亮
- 会话搜索
